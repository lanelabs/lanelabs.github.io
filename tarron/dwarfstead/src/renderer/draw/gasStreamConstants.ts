/**
 * Shared constants for gas stream drawing.
 * Beige/clay color palette for gas (inverted water).
 */

/** Pre-blended gas flow color: beige/clay at full alpha over cave bg. */
export const GAS_FLOW_COLOR = 0xa8916b;
export const GAS_FLOW_ALPHA = 1;

/** Pool color for settled gas. */
export const GAS_COLOR = 0xc4a882;

/** Splash accent for gas. */
export const GAS_SPLASH_COLOR = 0xd4b892;

/** Ribbon height as fraction of tile size (same as water). */
export const GAS_RIBBON_FRAC = 0.35;
/** Rise column width as fraction of tile size (same as water fall). */
export const GAS_RISE_FRAC = 0.40;
